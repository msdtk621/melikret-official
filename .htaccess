DirectoryIndex index.html

<IfModule mod_rewrite.c>
  RewriteEngine On

  # http -> https, www -> non-www を https://melikret.com へ301統一
  RewriteCond %{HTTPS} !=on [OR]
  RewriteCond %{HTTP_HOST} ^www\.melikret\.com$ [NC]
  RewriteRule ^ https://melikret.com%{REQUEST_URI} [R=301,L]
</IfModule>
